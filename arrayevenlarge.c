#include<stdio.h>
void main()
{
 int g,a=0, b[10],s=0,p=-32765;
 printf("Enter %d numbers  ",g);
 
 for(;a<g;a++)
 {
  scanf("%d",&b[a]);
 }
  printf("\n largest no. among all elements in the array is %d ",p);
 
}
